function getLength(str){
    console.log(str.length);
}
const value = getLength("Rohith S D");

function giveindex(str , target){
    console.log(str.indexOf(target));
}
const index = giveindex("Rohith Devaraju", "Devaraju");

function Slice(str, start, end){
    console.log(str.slice(start, end));
}
const slicedValue = Slice("Rohith S D", 0, 4);

